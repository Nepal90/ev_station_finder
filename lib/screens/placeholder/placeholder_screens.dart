import 'package:flutter/material.dart';

class FindStationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Find Stations')),
      body: Center(child: Text('Find Stations Screen')),
    );
  }
}

// class RoadmapScreen extends StatelessWidget {
//   @override
//   Widget build(BuildContext context) {
//     return Scaffold(
//       appBar: AppBar(title: Text('Roadmap')),
//       body: Center(child: Text('Roadmap Screen')),
//     );
//   }
// }
